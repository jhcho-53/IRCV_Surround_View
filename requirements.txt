# AVM/BEV pipeline runtime dependencies.
# Only two third-party packages are used; everything else is the Python standard
# library (argparse, csv, dataclasses, glob, os, shutil, sqlite3).
# Tested on Python 3.13, OpenCV 4.11.0, NumPy 2.5.1.

numpy>=2.0,<3

# OpenCV with the contrib modules: needs cv2.fisheye (Kannala) and
# cv2.aruco.CharucoDetector (OpenCV >= 4.7). "headless" = no GUI system libs
# (the pipeline only reads/writes images and video, never imshow).
opencv-contrib-python-headless>=4.10,<4.12
